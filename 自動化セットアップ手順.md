# CMEスクレイパー自動化セットアップ手順

## 📋 前提条件
- macOSを使用していること
- Pythonの仮想環境（venv）が作成済みであること
- `service_account.json`が配置済みであること

## 🚀 自動化のセットアップ手順

### ステップ1: ログディレクトリの作成
ターミナルで以下を実行：

```bash
cd /Users/suzukiyuu/Desktop/CME
mkdir -p logs
```

### ステップ2: plistファイルをlaunchdに登録
ターミナルで以下を実行：

```bash
# plistファイルをlaunchdのディレクトリにコピー
cp /Users/suzukiyuu/Desktop/CME/com.cme.scraper.plist ~/Library/LaunchAgents/

# launchdに登録
launchctl load ~/Library/LaunchAgents/com.cme.scraper.plist
```

### ステップ3: 動作確認
ターミナルで以下を実行して、登録されているか確認：

```bash
# 登録状況を確認
launchctl list | grep cme

# ログを確認（実行後）
tail -f /Users/suzukiyuu/Desktop/CME/logs/output.log
```

### ステップ4: 手動実行でテスト
ターミナルで以下を実行：

```bash
cd /Users/suzukiyuu/Desktop/CME
source venv/bin/activate
python main.py
```

正常に動作することを確認してください。

## ⏰ 実行スケジュール
スクリプトは以下の時刻に自動実行されます：
- **9:00** (日本時間)
- **15:00** (日本時間)
- **21:00** (日本時間)
- **3:00** (日本時間)

## 🔧 よくある操作

### 自動実行を停止する
```bash
launchctl unload ~/Library/LaunchAgents/com.cme.scraper.plist
```

### 自動実行を再開する
```bash
launchctl load ~/Library/LaunchAgents/com.cme.scraper.plist
```

### ログを確認する
```bash
# 正常な出力ログ
cat /Users/suzukiyuu/Desktop/CME/logs/output.log

# エラーログ
cat /Users/suzukiyuu/Desktop/CME/logs/error.log
```

### 設定を変更した場合（plistファイルを編集後）
```bash
# 一度停止
launchctl unload ~/Library/LaunchAgents/com.cme.scraper.plist

# plistファイルを再コピー
cp /Users/suzukiyuu/Desktop/CME/com.cme.scraper.plist ~/Library/LaunchAgents/

# 再度登録
launchctl load ~/Library/LaunchAgents/com.cme.scraper.plist
```

## ⚠️ 注意事項
- Macがスリープしている場合は実行されません（Macを起動したままにしておく必要があります）
- 実行時刻にMacが起動していない場合、その回の実行はスキップされます
- ログファイルは自動的に追記されるため、定期的に確認・削除することをおすすめします
